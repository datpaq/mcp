# Build artifacts
/bin/
/build/

# vhs per-frame PNG output (docs/cli-terminal.png is the dir name vhs
# uses when 'Output ….png' is set in the tape — 1500+ frames). The
# animated .webp / .gif are what we actually ship.
/docs/cli-terminal.png/
*.exe
*.test
*.prof
*.out

# Go workspace files
go.work
go.work.sum

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local config / secrets (never commit a real API key)
.env
.env.local
*.local

# Printing-press run state
.runstate/

# AI / agent tools (per-developer local state — project-level AGENTS.md
# stays tracked, since it's the shared Codex/agent contributor doc)
.claude/
.codex/
.cursor/
.copilot/
